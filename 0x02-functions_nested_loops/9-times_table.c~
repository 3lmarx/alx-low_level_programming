#include "main.h"

/**
 *times_table - prints the 9 times table
 *
 *Return: no return
 *
 */

void times_table(void)
{
int a, b, c;
 for (a = 0; a <= 9; a++)
   {
     _putchar(48);
     {
       for (b = 1; b <= 9; b++)
	 {
	   c = a * b;
	   
