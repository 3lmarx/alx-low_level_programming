#include <stdio.h>
#include "main.h"

/**
 *islower - check for lower case
 *
 *c - character to be checked
 *Return: Always 0
 *
 */

int islower(int c)
{
if (c >= 97 && c < 122)
{
return (1);
}
else
{
return (0);

}
_putchar("\n");
}
